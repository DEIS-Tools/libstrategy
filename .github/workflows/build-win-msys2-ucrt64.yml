---
name: Build for Windows-MSYS2-UCRT64
on:
  push:
    branches: [main]
    paths:
      - src/**
      - test/**
      - cmake/**
      - CMakeLists.txt
      - CMakePresets.json
      - .github/workflows/build-win-msys2-ucrt64.yml
  pull_request:
    branches: [main]
    types: [synchronize, opened, reopened, ready_for_review]
    paths:
      - src/**
      - test/**
      - cmake/**
      - CMakeLists.txt
      - CMakePresets.json
      - .github/workflows/build-win-msys2-ucrt64.yml
  workflow_dispatch:

jobs:
  windows-msys2-ucrt64:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    env:
      CMAKE_GENERATOR: "Ninja Multi-Config"
      CTEST_OUTPUT_ON_FAILURE: 1
      BUILD_DIR: build
    steps:
      - uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: git cmake ninja mingw-w64-ucrt-x86_64-gcc mingw-w64-ucrt-x86_64-boost
      - name: Setup Runner Environment
        run: |
          uname -a
          CORES=$(nproc)
          echo "CMAKE_BUILD_PARALLEL_LEVEL=$CORES" >> $GITHUB_ENV
          echo "CTEST_TEST_PARALLEL_LEVEL=$CORES" >> $GITHUB_ENV
          echo "CTEST_TEST_LOAD=$CORES" >> $GITHUB_ENV
      - uses: actions/checkout@v5
      - name: Configure Multi-Config
        run: cmake --preset multi-san
      - name: Build Debug
        run: cmake --build --preset debug-san
      - name: Test Debug
        run: ctest --preset debug-san
      - name: Build Release
        run: cmake --build --preset release-san
      - name: Test Release
        run: ctest --preset release-san
